[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "QM2BeamLine"
version = "1.0.0"
dependencies = [
    "nxrefine"
]

[project.entry-points."nxrefine.beamlines"]
beamline = "qm2beamline:QM2BeamLine"
